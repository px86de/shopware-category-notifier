{% if subscription.firstName %}Hallo {{ subscription.firstName }},{% else %}Hallo,{% endif %}

ein neues Produkt wurde in der Kategorie "{{ category.name }}" eingestellt:

{{ product.name }}
{% if product.description %}
{{ product.description }}
{% endif %}

Produkt ansehen: {{ productUrl }}

---
Sie erhalten diese E-Mail, weil Sie Benachrichtigungen für die Kategorie "{{ category.name }}" abonniert haben.
Möchten Sie keine weiteren Benachrichtigungen erhalten? {{ unsubscribeUrl }}


{% if firstName %}Hallo {{ firstName }},{% else %}Hallo,{% endif %}

ein neues Produkt wurde in der Kategorie "{{ category.translated.name ?? category.name }}" eingestellt:

{{ product.translated.name ?? product.name }}
{% if product.translated.description ?? product.description %}
{{ product.translated.description ?? product.description }}
{% endif %}

Produkt ansehen: {{ productUrl }}

---
Sie erhalten diese E-Mail, weil Sie Benachrichtigungen für die Kategorie "{{ category.translated.name ?? category.name }}" abonniert haben.
Möchten Sie keine weiteren Benachrichtigungen erhalten? {{ unsubscribeUrl }}
